NAZWA
        imageprocessor - process PBM and PGM images

STRESZCZENIE
        imageprocessor -h
        imageprocessor [-n] [-b] [-dn] [-g] [-e] [-d]
            [-i filename] [-o output] [-r width height] [-ib treshold] [-r angle]

OPIS
        imageprocessor przetwarza pliki graficzne grayscale i pliki graficzne binarne

FLAGI
       -n        - tworzy negatyw obrazu

       -b blur   - tworzy blur obrazu. mozliwe opcje 'gauss' albo 'average'

       -dn       - odszumia obraz

       -g        - tworzy obraz gradientowy. mozliwe 3 parametry '1' dla algorytmu Sobela, '2' dla algorytmu Prewitta, '3' dla algorytmu Robertsa

       -e        - tworzy erozje obrazu. tylko dla obrazow binarynch pbm

       -d        - tworzy dylatacje obrazu. tylko dla obrazow binarnych PBM

       -i filename    - plik do wczytania, filename to pelna sciezka pliku

       -o output      - plik po obrobce do zapisania

       -r width height  - zmien rozdielczosc pliku

       -ib treshold     - stworz obraz binarny. wartosci powyzej parametru 'treshold' zamieniaja sie na maksymalna wartosc pixela z naglowka, wartosci ponizej zamieniaja sie na 0

       -r angle         - obroc obraz o x stopni